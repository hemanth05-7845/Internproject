# Mafia Game Engine

Spring Boot service that owns the authoritative game logic and state transitions.

## Current Bootstrap

- Health endpoint
- Basic snapshot endpoint
- MongoDB connection configuration in `application.yml`

## Run

```bash
mvn spring-boot:run
```
